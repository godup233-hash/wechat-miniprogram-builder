---
name: wechat-miniprogram-builder
description: 从需求到交付构建、修复、测试和发布微信小程序。适用于将想法梳理为可控范围的 MVP、编写或修改原生微信小程序项目（WXML/WXSS/JavaScript/TypeScript）、选择本地或云端数据方案、排查微信开发者工具或真机问题、进行发布前隐私/域名/接口审计，以及指导体验版、上传和提审准备。
---

# 微信小程序构建

先交付一个可用的 MVP，再扩大范围。把用户当作产品负责人，把 AI 助手当作技术负责人：假设必须显式说明，平台后台的操作与代码修改严格分开，绝不编造标识符、凭证、资质或审核结果。

## 执行原则

- 除非既有项目已确立其他技术栈，否则一律使用原生小程序规范。禁止在未确认兼容性前使用浏览器专属的 DOM API、HTML、CSS 特性或依赖 hover 的交互。
- 首个版本只做一条完整核心流程，加上必要的空态/加载/错误状态。账号体系、社交、支付、复杂统计默认推迟，除非它们就是核心价值。
- 不得在公开仓库暴露 AppID、密钥、私钥、用户数据或生产环境 ID。使用文档化的占位符，或用 Git 忽略的本地配置。
- 平台规则、支持的 API、配额、准入条件、费用、审核周期都会变化。在建议用户提交前，必须以当前微信官方文档或后台为准进行核实。
- 当模拟器与真机表现不一致时，以原生组件和 API 文档为准。若界面文案或工具已更新，请用户截图并提供完整控制台报错。

## 1. 写代码前先框定版本

先用一句话复述需求，并提议一个 v1 方案。把缺失的决策**一次性批量**问清，每项都附上合理默认值，让用户可以快速"确认"或修改。

必须确认的决策：

1. 目标用户是谁，想完成什么任务。
2. 一个核心成功动作、输入字段，以及完成后的去向。
3. 页面、跳转关系，以及所有重要的点击/长按/滑动。
4. 数据字段、归属、保留/删除策略，以及数据是否需要卸载重装后保留、跨设备同步或共享。
5. 登录、位置、媒体、支付、通知等敏感能力是否需要。
6. 视觉方向：logo/素材、品牌色、文案语气、无障碍需求。
7. 发布目标：个人本地使用、邀请体验测试、公开发布，还是商业服务。

产出一份紧凑的产品简报，包含：范围与非目标、页面地图、用户流程、数据模型、验收标准、风险、未决选项。当用户需要可复制的提示词或需求访谈时，查阅 [prompt-templates.md](references/prompt-templates.md)。

## 2. 低成本验证方向

对新产品或视觉重要的产品，在创建原生项目前先用浏览器做一个核心流程的交互原型。它只用于验证布局、文案和状态流转；明确标注这是原型，不是小程序代码。

在正式实现前先制定视觉规范：带十六进制值的命名色板、间距比例、字号比例、组件状态、图片处理方式、可点击区域尺寸。如果已有 logo，从它推导克制的配色，而不是凭空猜颜色。

如果是迭代既有小程序，或只是范围很小的 bug 修复，可以跳过原型，但要记录原因。

## 3. 构建原生项目

修改前先勘察项目。识别其框架、包管理器、app/页面配置、组件、云函数、环境配置，以及已有的 lint/测试命令。遵守既有约定。

对新的原生项目，每个页面都要做到可测试的完整程度：

- 在 `app.json` 中注册路由；保持页面级 `.json`、`.wxml`、`.wxss`、`.js`/`.ts` 文件对应一致。
- 实现显式的加载、空态、校验错误、失败状态。对重复提交做禁用或防抖。
- 有意识地使用 `rpx` 响应式布局；在小屏和大屏设备视口上分别验证。不依赖鼠标 hover 或固定的桌面几何尺寸。
- 在样式跨页面复制之前，先集中管理主题变量和可复用组件。
- 在客户端和服务端/云函数边界都做输入校验。展示可操作的中文提示，而不是裸报错。
- 只引入当前流程所需的权限和 API。对每个敏感 API，记录用途、触发时机、降级方案和删除路径。

### 慎重选择数据存储

| 需求 | 默认方案 | 实现要求 |
| --- | --- | --- |
| 临时的、单设备原型 | 本地存储 | 键名加命名空间，处理损坏/缺失值，提供重置/导出指引。 |
| 持久的或跨设备的个人数据 | 云开发或已备案后端 | 先建模数据归属和访问规则；特权操作放在服务端。 |
| 共享、内容审核、付费或受监管的数据 | 经过明确设计评审的合规后端 | 编码前先定义授权、审计、删除、防滥用和合规约束。 |

绝不能因为客户端调用存在就声称云数据已配置好。把环境 ID 记录为用户提供的配置，只创建所需的集合/函数，应用最小权限规则，并在真机上测试真实的持久化路径。

## 4. 按可观测的切片开发

一次只实现一条完整的用户流程，核心页面优先于装饰性的落地页。每个切片完成后：

1. 在微信开发者工具中编译。
2. 在模拟器中走一遍验收标准。
3. 检查布局、文字截断、点击行为、素材加载、加载/错误/空态，以及（适用的）持久化。
4. 记录真实报错并修复根因，然后重新跑完整核心流程回归——而不只是改动的那一项。

处理缺陷报告时，必须收集：精确的复现步骤、期望与实际行为、完整的控制台/报错原文、设备/系统/微信版本（如仅真机出现）、截图或视频。不要凭转述的部分报错就下诊断。

当浏览器实现方式不兼容时，用原生组件/API 模式替换，而不是压制警告。当代码修改看似未生效时，先显式重新编译/刷新，确认被改文件确实属于当前导入的项目，再检查缓存/构建产物，不要贸然做无关改动。

## 5. 真机测试

按问题类型选择开发者工具的功能：

| 目标 | 使用方式 | 需收集的证据 |
| --- | --- | --- |
| 快速视觉检查 | 预览 | 截图和构建标识 |
| 跨平台表现 | 有代表性的 iOS 和 Android 真机调试/预览 | 设备、系统、微信版本、通过/失败记录 |
| 仅真机出现的缺陷 | 远程调试 | 完整日志、复现步骤、截图 |

在任何构建对外分享前，执行核心流程回归：新建/添加、编辑或改变状态、杀进程重启验证持久化、删除/重置、非法/空输入、（相关的）断网或请求失败，以及完整的返回导航路径。把产品特有的边界情况补充进产品简报。

## 6. 分享、上传或提审前先审计

先跑仓库自带的审计脚本：

```bash
python3 scripts/audit_miniprogram.py /项目绝对路径 --format markdown
```

如果项目在别的位置，从本 skill 的 `scripts/` 目录调用该脚本。它是静态盘点，不是认证：逐条核对其发现，把无法验证的证据标记为 `未验证`，绝不能当作通过。

然后准备一份包含四个责任方的发布清单：

- **代码：** 编译通过；路由可用；报错/日志已审阅；生成/测试文件已排除；AppID 是有意配置的；仓库未追踪任何密钥。
- **网络与云：** 按 API 类型列出所有外部域名；要求安全端点并在当前后台完成相应配置；核实云环境、数据规则和已部署的函数。
- **隐私：** 盘点所有信息/权限相关 API 及其文件与行号；把每一项映射到用户可见的用途和当前的隐私声明；移除未使用的采集。
- **平台后台：** 由账号所有者核实主体、服务类目、基本信息、截图、测试入口、备案/认证要求，以及当前微信后台中的提审文案。

用审计脚本生成盘点，并在指导发布前阅读 [release-playbook.md](references/release-playbook.md)。明确区分：`代码已改`、`需在后台操作`、`未验证`、`受阻`。

## 7. 干净地交接

交付项目路径、导入方式、所需的配置项（不含密钥）、已实现的 v1 范围、带设备覆盖的测试结果、已知限制，以及确切的后续动作。写审核文案时，如实、具体地描述审核者会看到什么、能做什么、结果是什么。

不要承诺"已发布""体验版已公开可访问"或"审核会通过"。这些是平台控制的状态，必须由账号所有者确认。

## 参考资料与工具

- [prompt-templates.md](references/prompt-templates.md)：需求访谈、原型、分片实现、报错修复、提审审计的提示词模板。
- [release-playbook.md](references/release-playbook.md)：静态审计解读与发布证据清单。
- [audit_miniprogram.py](scripts/audit_miniprogram.py)：离线静态盘点工具，覆盖路由、配置、网络调用、隐私相关 API 和调试日志。
